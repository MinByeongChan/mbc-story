# Resume

`apps/dev-blog`의 이력서 페이지를 독립 실행할 수 있도록 옮긴 Next.js 앱입니다.

운영 주소는 `https://byeongchan.space/resume`이며, Next.js `basePath`도 `/resume`으로 설정합니다.

```bash
pnpm --filter resume dev
pnpm --filter resume typecheck
pnpm --filter resume build
```
