# Resume

`apps/dev-blog`의 `/about` 이력서 페이지를 독립 실행할 수 있도록 옮긴 Next.js 앱입니다.

기존 페이지 컴포넌트와 스타일은 그대로 복사했으며, `/`와 `/about`에서 같은 이력서를 제공합니다.

```bash
pnpm --filter resume dev
pnpm --filter resume typecheck
pnpm --filter resume build
```
